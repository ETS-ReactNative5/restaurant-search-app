import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    paddingTop: 10,
    paddingLeft: 8,
    fontSize: 18,
    fontFamily: "roboto",
    fontWeight: "300",
  },
});
